# AI_HANDOFF

更新日期：2026-08-08

## 当前状态

- 项目：Neon Skies，Git 分支 `main`。
- 最近代码提交：当前 v0.9.0 发布提交以 Git 历史为准。
- 当前阶段：v0.9.0 发布后的长期维护。
- 技术形态：原生 HTML5/CSS/ES Modules + Canvas 2D + Web Audio，无 npm 依赖、无构建步骤、无数据库。
- 本轮功能：已加入浏览器本地存档；普通模式改装界面提供 10 个机库槽位并自动保存当前槽位，支持点击名称重命名和槽位配置码直接导入/导出，机体名称直接占用槽位标题行并使用白色粗体显示，光锥之外改装界面不显示机库，未完成航程使用独立本地安全断点。
- 发布部署：已加入 `.github/workflows/deploy-pages.yml`，GitHub Pages 从 `main` 分支自动部署完整源码。
- 工作区：本轮开始前已有用户修改的 `.agents/skills/local-save/SKILL.md` 与 `.agents/skills/publish-release/SKILL.md`，按工作区归属规则保留且不纳入本次发布；v0.9.0 更新日志已确认，发布提交尚未创建。生成的 `publish-package/` 和 ZIP 被 Git 忽略，不能作为源码基线。

## 最近完成

- v0.9.0 已包含星弧、静默磁暴、战术地雷，以及调整为史诗标准投放的光学迷踪和幽蓝奇点；并保留天幕协议、黑曜波束、星蚀脉冲、星环、火羽及暗蚀连携。
- 当前配置确认 100 个关卡、20 个标准 Boss 关、39 个深空事件、4 档光锥之外难度、3 档躲避弹幕难度。
- 5 个仍为传说稀有度、正常模式已开放但此前未进入 Beyond 普通投放的模块，现通过稀有传说事件定向奖励；已改为史诗的光学迷踪与幽蓝奇点不再使用专属事件或首领固定奖励，而是进入标准史诗模块池。
- 当前工作区新增 3 个玩家可用模块：稀有自动模块“星弧”、电属性史诗主动模块“静默磁暴”和“战术地雷”；星弧向前发射弧光子弹，静默磁暴提供全屏磁暴爆发和持续领域，战术地雷在模块位置部署并使用 3 层、6 秒间隔充能，每枚最多存在 30 秒。三者均未加入禁用集合，按正常稀有/史诗模块规则进入光锥之外标准奖励池，不使用传说模块的定向事件投放。
- 已建立并同步根目录入口文档和 `docs/` 专题文档。
- 已修正事件档案数量文案为 39，并移除已删除加载界面的残留入口和样式。
- 已加入 `LocalSaveSystem`：普通模式十个机库槽位、选中槽位自动保存，以及光锥之外航程的本地安全断点与继续入口。
- 机库槽位已支持最多 24 个字符的名称，旧的无名称槽位会自动使用默认名称。
- 机库槽位通过统一的“配置码”按钮导出或导入；导入后自动切换到目标槽位。
- 已修复机库名称输入时 `InputSystem` 全局数字技能快捷键拦截的问题；输入框聚焦时数字键交给文本输入。
- GitHub Pages 之前未连接自动部署；本次工作流推送后需以 Actions 运行结果和线上版本号确认部署完成。
- 已修复改装界面工具按钮在移动端触控目标过小的问题，并为仅提供 Touch Events 的旧移动浏览器补充按钮释放处理；手机端保留原紧凑视觉样式，仅扩展透明触控命中区域，普通模式机库按钮仍只在普通改装界面显示。
- 已将“每次正式发布必须完成手机端触控回归”写入 `docs/DEVELOPMENT_RULES.md` 和 `docs/TEST_CHECKLIST.md`。
- 已调整暗蚀连携：暗耀印记可无限叠加，星蚀脉冲按印记层数分别触发 15 点暗属性范围爆炸并清除印记；火羽命中敌人后立即消失，不再穿透。

## 最近验证

- 当前工作区最近一次逻辑测试结果为 `All tests passed`，共 82 项测试；对应记录见 `docs/TEST_RESULTS.md`。本轮已完成移动视口下主菜单、模式选择、改装入口、模块详情弹窗关闭和触摸拖动冒烟；主动技能按钮因内置浏览器无法可靠完成主动模块落位，已记录为环境限制。
- `src/ui/UI.js`、`src/main.js` 通过 `node --check`，工作区差异通过 `git diff --check`。
- v0.9.0 发布目录已重新生成并检查，目录与 ZIP 各含 40 个文件，入口、样式、主逻辑、光锥之外逻辑、资源和音频均已包含；测试页显示 `All tests passed`。

## 当前任务与下一步

本轮本地存档、机库、光锥之外模块投放、低稀有度模块和开发规则同步已完成。当前下一步是完成 v0.9.0 发布并确认 Pages 部署结果：

1. 合成系统暂时停止开发；不要仅凭已有代码自行开放，也不要把它称为当前玩家的“合成模块”。
2. 本次 v0.9.0 更新日志已由用户确认，提交与推送完成后以 Git 历史和 Pages 工作流为准。
3. 移动端主动技能按钮仍需在真实手机或可靠移动端模拟环境补做回归；不要把当前内置浏览器限制写成代码已验证。

## 风险

- `tests/logic-tests.js` 已按当前 v0.9.0 规则同步，浏览器测试页当前显示 `All tests passed`；后续版本改动必须先更新对应断言再判断回归。
- 合成相关实现和旧定义仅作暂停开发的内部存放；玩家模块库、百科、商店、Boss 奖励和光锥之外奖励池均不包含它们。
- 用户已确认当前 GitHub 部署版本在手机和电脑上正常运行；未指定具体设备和版本，不得扩大为完整兼容承诺。
- 配置码和航程存档依赖模块 ID、share code、位置、旋转和字段兼容；不要做无理由的标识清理。
- 浏览器本地存档只在当前浏览器设备和站点来源内有效；清理网站数据或更换设备后，普通机体需用配置码迁移，光锥之外航程需用航程码迁移；当前没有云端同步。
- `CHANGELOG.md` 有项目级确认规则，修改前必须先列完整候选条目并等待用户确认。
- 正式发布必须使用用户提供的版本号；用户确认该版本更新日志后，才可提交、推送 GitHub 并触发 Pages 工作流。
- UI 有动态注入元素，不能只检查 `index.html` 静态 ID；要同时检查 `UI.js` 和 `BeyondLightConeUI.js`。

## 建议新 AI 先检查

```powershell
git status --short
git log --oneline -20
powershell -ExecutionPolicy Bypass -File .\start-server.ps1
```

然后阅读 `AGENTS.md`、`docs/DEVELOPMENT_RULES.md`、`docs/ARCHITECTURE.md`、`docs/KNOWN_ISSUES.md`，再打开主页面和 `tests/index.html`。若任务涉及功能，继续阅读 `FEATURES.md` 和 `DATA_MODEL.md`；如果修改了规则或断言，必须同时更新 `docs/TEST_CHECKLIST.md`。

## 什么时候更新本文件

每次正式开发、修复、测试基线变化、风险变化、版本发布或任务交接结束时更新。至少写清：当前提交/阶段、最近完成、当前任务、下一步、风险、验证结果和遗留问题。不要把聊天记录当唯一状态来源。
