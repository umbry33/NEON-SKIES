# 开发说明

本文件只保留入口说明，不定义当前版本的开发任务或通用约束；正式规则以 `AGENTS.md` 和 `docs/DEVELOPMENT_RULES.md` 为准，未来计划以 `docs/ROADMAP.md` 为准。

## 历史计划说明

原有开发顺序中的已完成或已过期条目不再作为后续任务清单；仍有价值的未来意图统一维护在 `docs/ROADMAP.md`，每项是否进入开发必须经过具体任务确认。新内容应保持数据驱动：通常新增模块只需要配置和一个行为实现，不应在 `Player` 或 `Game` 中散落大量模块专用分支。

开始开发前先读 `AGENTS.md`、`docs/AI_HANDOFF.md` 和 `docs/DEVELOPMENT_RULES.md`。
